public static void main(String[]strings) {
	 double mininyear = 60*24*365;
     Scanner input = new Scanner(System.in);
     Scanner.out.print("give your minutes: ");
     double minutes=input.nextDouble();
     long years=(long)(minutes/mininyear);
     int days=(days)(minutes/minutes/60/24)%365;
     System.out.println((int)minutes +"minutes is nearly "+ "years and"+ days +"days");
	}
